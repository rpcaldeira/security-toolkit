sudo: false
language: java
jdk:
  - openjdk11
addons:
  sonarcloud:
    organization: "rpcaldeira-github" # the key of the org you chose at step #3
    token:
      secure: ${SonarCloud} # encrypted value of your token
script:
  - mvn clean verify sonar:sonar -Pcoverage